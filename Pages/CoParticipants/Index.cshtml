@page
@model EventAppEFCore.Pages.CoParticipants.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyParticipant[0].CoCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyParticipant[0].CoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyParticipant[0].CoParticipants)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyParticipant[0].AdditionalInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyParticipant[0].PaymentType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CompanyParticipant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CoCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoParticipants)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
